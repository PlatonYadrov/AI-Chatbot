fastapi==0.110.0
uvicorn==0.29.0
requests==2.32.3
langchain==0.2.6
langchain-community==0.2.6
langchain-core==0.2.10
qdrant-client==1.9.1
python-dateutil>=2.8.2
openai>=1.12.0

# Logging and caching
redis>=5.0.0
loguru>=0.7.0

# PostgreSQL for session storage (async)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# ✅ BM25 для гибридного поиска (легковесная библиотека)
rank-bm25>=0.2.2

# ✅ Cross-Encoder reranking via Infinity container (no torch needed in Python!)
# All heavy ML inference happens in the Infinity container
# torch, transformers, sentence-transformers are NOT required here

# Optional: Uncomment if you want local fallback reranking with torch
# (Not recommended - use Infinity container instead)
# sentence-transformers>=2.7.0
# torch>=2.0.0
# numpy>=1.24.0
# transformers>=4.35.0

