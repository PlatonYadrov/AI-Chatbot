# FastAPI & Web Framework
fastapi==0.110.0
uvicorn==0.29.0
python-multipart==0.0.9

# HTTP & Network
requests==2.32.3

# Vector Database & Embeddings
qdrant-client==1.9.1
langchain==0.2.6
langchain-community==0.2.6

# Data Processing
numpy==1.26.4
pydantic==2.7.1
tiktoken==0.6.0
pyyaml==6.0.1

# Deduplication & Language Detection
datasketch==1.6.4
langdetect==1.0.9

# Docling - Unified Document Processing (replaces all legacy parsers)
# Includes: PDF, DOCX, PPTX, XLSX, HTML, Images, OCR, Tables, Formulas
docling>=2.0.0
docling-core[chunking]>=2.0.0

# PDF generation for model download testing
reportlab>=4.0.0

# Optional: VLM for advanced image understanding (requires GPU)
# Uncomment ONLY if you have spare VRAM (4-6GB) after vLLM allocation
# docling[vlm]>=2.0.0
transformers
sentencepiece
tiktoken
langchain