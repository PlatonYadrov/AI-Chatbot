# Пример файла окружения для сервисов

# Токен доступа к Confluence (используется сервисом confluence_sync)
# Скопируйте эту строку в корневой .env и подставьте реальный токен
TOKEN=REPLACE_ME_CONFLUENCE_BEARER_TOKEN


